# WP10 RED contract

The test-only checkpoint defines accessibility and responsive behavior that the current WP09 production surface does not yet satisfy.

Expected missing contracts before the WP10 production commit:

- named atomic `Current application status` region;
- `aria-busy` on approval and confirmation dialogs;
- non-announcing approval countdown timer semantics;
- atomic `plan-announcement` live region;
- 44-pixel critical interaction targets at the frozen viewports.

GitHub Actions is not used as the implementation gate. The source gap was confirmed directly against the exact WP09 head before this test-only commit.
