# WP11 RED contract

The plain-Node verification contract was executed against an empty WP11 staging root before implementation.

Observed result:

```text
AssertionError: WP11_REQUIRED_FILE_MISSING:tests/README.md
exit code 1
```

The production checkpoint must add all verification scripts, 30 evaluation cases, 24-scenario traceability, release-manifest support, and package commands before this marker is removed.
