import assert from 'node:assert/strict';
import { createHash } from 'node:crypto';
import { readdirSync, readFileSync, statSync } from 'node:fs';
import { join, relative } from 'node:path';
import { FROZEN_FIXTURE_HASH, gitCommit, readToolMetadata, sha256File } from './lib/source-metadata.mjs';
function directoryHash(directory) { const files = []; function walk(path) { for (const name of readdirSync(path).sort()) { const child = join(path, name); if (statSync(child).isDirectory()) walk(child); else files.push(child); } } walk(directory); const hash = createHash('sha256'); for (const path of files) { hash.update(relative(directory, path)); hash.update('\0'); hash.update(readFileSync(path)); hash.update('\0'); } return hash.digest('hex'); }
const root = process.cwd(); const manifest = JSON.parse(readFileSync(join(root, 'release-manifest.json'), 'utf8')); const { contracts, lifecycle } = readToolMetadata(root);
assert.equal(manifest.schemaVersion, 1); assert.equal(manifest.product, 'DOMHamster'); assert.equal(manifest.sourceCommit, gitCommit(root), 'DOMHAMSTER_RELEASE_COMMIT_MISMATCH'); assert.equal(manifest.fixture.sha256, FROZEN_FIXTURE_HASH, 'DOMHAMSTER_FIXTURE_HASH_MISMATCH'); assert.deepEqual(manifest.tools.names, contracts.map(({ name }) => name)); assert.deepEqual(manifest.tools.lifecycleCounts, Object.fromEntries(Object.entries(lifecycle).map(([state, names]) => [state, names.length]))); assert.equal(manifest.hashes.packageLockSha256, sha256File(join(root, 'package-lock.json'))); assert.equal(manifest.hashes.contractsSha256, sha256File(join(root, 'src/webmcp/contracts.ts'))); assert.equal(manifest.hashes.lifecycleSha256, sha256File(join(root, 'src/webmcp/lifecycle.ts'))); assert.equal(manifest.hashes.distSha256, directoryHash(join(root, 'dist'))); console.log(`DOMHAMSTER_RELEASE_VERIFIED commit=${manifest.sourceCommit}`);
