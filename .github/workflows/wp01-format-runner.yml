name: WP01 one-shot branch formatter

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/wp01-format-runner.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  format-wp01:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out WP01 branch
        uses: actions/checkout@v7
        with:
          ref: implementation/wp01-domain-fixture
          fetch-depth: 0

      - name: Set up Node.js 24.19.0
        uses: actions/setup-node@v7
        with:
          node-version: 24.19.0
          cache: npm

      - name: Install committed dependencies
        run: npm ci --ignore-scripts

      - name: Format WP01 sources
        run: npm run format

      - name: Remove branch-only formatter and publish style changes
        shell: bash
        run: |
          rm -f .github/workflows/wp01-format.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          if git diff --cached --quiet; then
            echo 'No formatting changes were required.'
            exit 0
          fi
          git commit -m 'style: format WP01 domain fixture'
          git push origin HEAD:implementation/wp01-domain-fixture
