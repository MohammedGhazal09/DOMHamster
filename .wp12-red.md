# WP12 RED contract

The deployment contract was executed before Netlify configuration or compatibility records existed.

Observed result:

```text
AssertionError: WP12_REQUIRED_FILE_MISSING:netlify.toml
exit code 1
```

The production checkpoint must add source-controlled Netlify build, redirect, caching, security, WebMCP permission, release-identity, and official-client evidence templates before this marker is removed.
