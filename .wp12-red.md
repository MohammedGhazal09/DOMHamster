# WP12 RED contracts

Two plain-Node contracts were executed before deployment production files were published.

## Deployment configuration RED

```text
AssertionError: WP12_REQUIRED_FILE_MISSING:netlify.toml
exit code 1
```

## Release identity RED

The second contract was run against the exact WP11 release scripts and schema:

```text
AssertionError: WP12_RELEASE_CREATE_CONTRACT_MISSING:IGNORED_DIST_FILES
exit code 1
```

The production checkpoint must add source-controlled Netlify build, redirect, caching, security, WebMCP permission, official-client evidence templates, and a non-circular deployed release manifest before this marker is removed.
